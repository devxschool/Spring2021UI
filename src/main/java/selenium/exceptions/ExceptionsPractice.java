package selenium.exceptions;

import org.junit.Test;

public class ExceptionsPractice {


    //No Such element exception -> when any given locators like by id, name, xpath,
    // cssSelector do not point to any particular web element in the dom (document object model) source view(html).

    //timeout exception -> when the web is loading and implicit or explicit waits passed.

    //NoSuchWindowException -> when you are trying to swicth to another which doesnt exist

    //StaleElementException -> when we successfully located an element and the dom has refreshed or the page switched
    //reference to that web element becomes stale.


}
